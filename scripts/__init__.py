"""Repository tooling: pre-commit hooks and release helpers.

A package rather than loose files so the parts with tests can be imported by
name.  Nothing here ships in the wheel; see ``[tool.hatch.build.targets.wheel]``.
"""
