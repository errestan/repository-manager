"""Repository index generation.

Checked under mypy ``strict`` (see pyproject.toml): a type error here produces a
malformed index, which clients report as an unhelpful "Hash Sum mismatch" long
after the upload that caused it.
"""
